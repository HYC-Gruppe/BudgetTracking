package capgemini.hyc.BudgetTracking.jira.reports;

import org.junit.After;
import org.junit.Before;
import org.junit.Test;
import capgemini.hyc.BudgetTracking.jira.reports.BudgetTracking;

import static org.mockito.Mockito.*;

/**
 * @since 3.5
 */
public class BudgetTrackingTest {

    @Before
    public void setup() {

    }

    @After
    public void tearDown() {

    }

    @Test(expected=Exception.class)
    public void testSomething() throws Exception {

        //BudgetTracking testClass = new BudgetTracking();

        throw new Exception("BudgetTracking has no tests!");

    }

}
